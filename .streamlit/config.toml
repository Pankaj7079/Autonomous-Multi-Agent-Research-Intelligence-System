# streamlit's own chrome reads these, so widgets match the tokens in frontend/styles.py
[theme]
base = "dark"
primaryColor = "#4da6ff"
backgroundColor = "#0a0d13"
secondaryBackgroundColor = "#11151d"
textColor = "#dde5f0"

[server]
headless = true

[browser]
gatherUsageStats = false
