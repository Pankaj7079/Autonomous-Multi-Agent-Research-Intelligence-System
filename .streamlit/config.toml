# streamlit's own chrome reads these, so widgets match the tokens in frontend/styles.py
[theme]
base = "light"
primaryColor = "#0f766e"
backgroundColor = "#faf9f7"
secondaryBackgroundColor = "#f5f3f0"
textColor = "#1c1a17"

[server]
headless = true

[browser]
gatherUsageStats = false
